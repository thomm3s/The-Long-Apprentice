# Progress & Task Log — The Long Apprentice

*Working memory for automated hourly sessions. Read this first, update it last.*

## How this file is used

Every hourly session works through **3 Task Queue items, one after another**, each scoped small (roughly what used to be "one ~20-minute item," which in practice runs more like 5 minutes — the point is item *scope*, not a literal clock budget):
1. Read this whole file.
2. If the **Task Queue** has fewer than ~3 items left (including empty), top it up first: open `The Long Apprentice - Brief.md` section 10 (Build Order/Milestones), find the next unstarted **Phase**, and break it into 5-10 concrete, small (~20-minute) action items appended to the Task Queue below. Re-check queue depth before each of the 3 items below and top up again if it runs dry mid-session. If decomposition alone eats the whole time budget, that's a valid session on its own — stop there without touching code.
3. Do the following 3 times in a row (skip remaining repeats and stop early if you hit a genuine blocker or the overall time budget):
   a. Pick the top unchecked item in **Task Queue** and implement it (~20 min, one focused vertical slice — don't try to finish a whole milestone in one item).
   b. Validate changes (see Validation below). Don't move on if validation fails — fix it or stop and log the blocker.
   c. Commit to git with a clear message for that item.
   d. Check off that item in the Task Queue below before starting the next one.
4. After the 3 items (or however many you completed before stopping), add ONE **Session Log** entry covering all of them, update Current Status if it meaningfully changed, and add any new sub-tasks discovered.
5. Stop. Do not start a 4th item even if time remains.

If a queue item is too big to finish in 20 minutes, split it into a smaller sub-task, do the sub-task, and leave the rest in the queue (this still counts as one of the 3). When a Phase from the Brief is fully decomposed and completed, mark it done in the Brief's section 10 (or note it here) and move to the next Phase.

## Validation (do this before committing)

Since 2026-07-10 (inventory session onward), these sessions have run directly on Thijs's persistent Windows machine, not an ephemeral sandbox — the project folder, git, and the real Windows Godot editor binary are all just there and working normally. Use the local editor binary directly:
```
GODOT="C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe"
"$GODOT" --headless --import --path "<project folder>"
```
(If Thijs upgrades his local Godot version, update this path and the version numbers throughout this file to match.)
Then write/reuse a small `SceneTree` script to `ResourceLoader.load()` and `.instantiate()` any scenes touched this session, and check exit code / stderr for errors. Don't commit if the project doesn't import cleanly or a touched scene fails to load.

**Fallback (only if a future session is genuinely a fresh ephemeral sandbox with no persistent filesystem):** a Linux Godot binary can be cached at `.godot-tools/Godot_v4.7-stable_linux.x86_64` (gitignored) — download it from `https://github.com/godotengine/godot/releases/download/4.7-stable/Godot_v4.7-stable_linux.x86_64.zip` if missing, matching Thijs's local Windows version, and run the same `--headless --import` against it. As of this writing this fallback hasn't actually been needed since the very first session.

**Git note:** the repo has a GitHub remote (`origin`) but sessions should only `git add` the specific touched files and `git commit` locally — never `git push` automatically. Git has worked normally in every session since 2026-07-10 (inventory onward). The only failure was in the very first session (2026-07-10, "chop tree") — attributed to a one-off stale sandbox-mount-cache glitch that has not recurred since; it does not describe the current environment. If `git status` ever does error out, don't try to fix `.git` by deleting/reiniting it — just skip git for that run, do the file edits anyway, and note it clearly in the Session Log (it would now be worth investigating rather than assuming, since it hasn't been the norm).

## Current Status (as of 2026-07-11, updated same day — combat verb / Stats.gd hunger+stamina session)

Phase 0 (gray-box prototype) is complete. **Phase 1 — Skill-by-practice foundation is now fully complete**: three practiced verbs (chopping, running, combat), a perk/threshold unlock communicated via HUD progress + on-screen toast, and locked-down skill-decay/diminishing-returns rules. Actively starting **Phase 2 — Survival & atmosphere basics**: a new `Stats.gd` autoload now tracks `hunger` (decays continuously) and `stamina` (drains while sprinting, regenerates otherwise), both 0-100 and clamped, but neither has UI or a gameplay consequence wired up yet — those are the next queue items. The queue has 5 Phase 2 items remaining (HUD bars, hunger-zero consequence, day/night cycle, WorldEnvironment mood pass, sleep/bed interaction).

Prototype scaffolding exists and loads cleanly: `project.godot`, `scenes/player/Player.tscn`, `scenes/world/Main.tscn`, `scripts/Player.gd` (basic `CharacterBody3D` movement + raycast interact + raycast block placement), `scenes/props/Tree.tscn`, `scripts/Tree.gd` (chop-able placeholder tree), `scenes/props/Block.tscn` (placeholder placed-block cube, no script, group `placed_block`), `scripts/Inventory.gd` (autoload singleton, numbers-only item counts), `scenes/ui/HUD.tscn` + `scripts/HUD.gd` (CanvasLayer wood-count label, live-updates off `Inventory.changed`) — all verified via headless import + instantiate on Godot 4.7, no errors. Project upgraded from 4.3 to 4.7 on 2026-07-10 (config/features auto-bumped by the engine on import). Player can walk up to a Tree instance in Main.tscn and press E to chop it (removes tree, calls `Inventory.add("wood", 1)`, reflected live in the on-screen HUD label). Player can also right-click (new `place_block` input action) to raycast against the world and spawn a `Block.tscn` instance at the hit point (offset by the surface normal), decrementing wood by 1; does nothing if wood count is 0. Chopping a tree now also increments a hidden `Skills` autoload counter for `"chopping"`, printing a milestone line every 10 practices (no levels/perks yet — but see Brief section 4 for the now-locked design on how thresholds/decay/communication should eventually work). The game now boots into a placeholder title screen (`scenes/ui/TitleScreen.tscn`) instead of straight into `Main.tscn` — `project.godot`'s `run/main_scene` was repointed there; a "Start" button switches to `Main.tscn` via `get_tree().change_scene_to_file()`.

Git is initialized with a GitHub remote (`origin` -> thomm3s/The-Long-Apprentice). Each session commits locally after validating its change; nothing auto-pushes. Sessions run directly on Thijs's persistent Windows machine (not an ephemeral sandbox), with git and the real Windows Godot 4.7 editor binary (`C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`) both working normally — this has held for every session since 2026-07-10 (inventory onward). The mount-cache/git glitch mentioned in the Session Log was a one-off in the very first session and hasn't recurred.

The two stray placeholder files (`scripts/_probe_test.gd`, `_validate.gd`) have been deleted — they were confirmed inert and unreferenced.

## Task Queue (priority order — top first)

- [ ] Phase 3 — Player death/respawn flow: on the player's `Health.died`, reset position to spawn, refill health/hunger/stamina, and show a HUD toast ("You died") — no death screen yet, just the loop closing so dying isn't a dead end.

Phase 0 (gray-box prototype: move, chop a cube tree, pick up wood, place one building piece) is complete per the Completed list below. Phases 1 (skill-by-practice foundation) and 2 (survival & atmosphere) are complete. **Phase 3 — Combat & first enemy** has been decomposed into the 7 items above (2026-07-11 extended session). Full phase list: 0 gray-box prototype (done), 1 skill-by-practice foundation (done), 2 survival/atmosphere (done), 3 combat (active), 4 magic, 5 crafting/building depth, 6 hand-built biome, 7 procgen, 8 story/NPCs, 9 polish/UX, 10 multiplayer, 11 platform/release prep, 12 post-launch.

## Completed

- [x] 2026-07-11 — Player health + HUD bar: `scripts/Health.gd` (new `health_changed(new_health)` signal emitted on every mutation path — damage, heal, refill — so UI needs just one subscription), `scenes/player/Player.tscn` (`Health` child node added, default 100 max), `scenes/ui/HUD.tscn` (`HealthLabel` + `HealthBar` below the stamina bar, same `ProgressBar` pattern), `scripts/HUD.gd` (`_connect_player_health()` in `_ready` finds the player via the `player` group — guarded no-op if the scene has no player — seeds the bar from current health and subscribes to `health_changed`; works because Player is instanced before HUD in `Main.tscn`, so its `_ready` runs first). The live signal→bar wiring needs the usual manual click-through since headless probes can't pump frames.
- [x] 2026-07-11 — Enemy melee attack: `scripts/Enemy.gd` (`MELEE_REACH = 2.0` — slightly past keep-distance so the enemy can hit from where the chase stops — `MELEE_DAMAGE = 10`, `MELEE_COOLDOWN = 1.5s`; `_physics_process` ticks the cooldown down and, when the static pure `_melee_ready(distance, cooldown)` says go, calls `_melee_attack(player)` which starts the cooldown and damages the target's `Health` child *if it has one* — the player doesn't yet (next queue item), so until then swings are harmless by design, no crash).
- [x] 2026-07-11 — Enemy chase AI: `scripts/Enemy.gd` (`_physics_process` now applies gravity, looks up the player via `get_tree().get_first_node_in_group("player")`, and moves at `SPEED = 3.0` along `_chase_direction(from_pos, target_pos)` — a pure `static func` returning the flat XZ unit direction, or `ZERO` outside `AGGRO_RANGE = 12` or within `KEEP_DISTANCE = 1.5`; made static/pure specifically so it's probe-able headless, see the new Notes entry), `scenes/player/Player.tscn` (Player node added to new `player` group for the lookup).
- [x] 2026-07-11 — `Enemy.tscn` placeholder: `scenes/props/Enemy.tscn` (new — `CharacterBody3D` red box, group `damageable`, `Health` child with `max_health = 50` so it dies to two player swings), `scripts/Enemy.gd` (new — connects `Health.died` → emits `enemy_died` and `queue_free()`s), `scenes/world/Main.tscn` (one Enemy instance at (8, 0, -10), away from spawn). No AI yet — it stands there and can be killed.
- [x] 2026-07-11 — Attack hit detection: `scripts/Player.gd` (the three duplicated camera-raycast blocks — interact, place, attack — were consolidated into one `_camera_raycast(max_range)` helper that also excludes the player's own body via `query.exclude = [get_rid()]`; `_try_attack()` still practices "combat" on every swing — whiffs train too — then raycasts `ATTACK_RANGE = 2.5` and calls `take_damage(ATTACK_DAMAGE = 25.0)` on any hit body in group `damageable` with a `Health` child).
- [x] 2026-07-11 — Reusable `Health.gd` component: `scripts/Health.gd` (new — plain `Node` script, `@export max_health`, `take_damage()` clamps to 0 and emits `damaged(new_health, amount)` then `died` exactly once (further damage after death is ignored until refilled), `heal()` clamps to max, `refill()`; the owning body decides what dying means by listening to `died`). Not attached anywhere yet — the enemy and player items pick it up next.
- [x] 2026-07-11 — Sleep/bed interaction: `scenes/props/Bed.tscn` (new — `StaticBody3D` placeholder box bed with a brown material, group `sleepable`, following `Tree.tscn`'s reusable-scene pattern), `scripts/Bed.gd` (new — `sleep_in()` calls `TimeOfDay.skip_to_morning()`, restores hunger +50 partial / stamina fully, emits `slept`), `scripts/Player.gd` (`_try_interact()` restructured to early-return on no raycast hit, then branch by group: existing `choppable` branch unchanged, new `sleepable` branch calls `sleep_in()`), `scenes/world/Main.tscn` (one Bed instance at (-4, 0, -4) near player spawn). Design choices made autonomously: stamina restores fully (sleep should), hunger only partially (+50) so sleeping isn't a free replacement for eating. This closes Phase 2 — all survival/atmosphere items done.
- [x] 2026-07-11 — `WorldEnvironment` mood pass: `scenes/world/Main.tscn` (the scene already had a `WorldEnvironment` node from the original gray-box setup — its `Environment` sub-resource was tuned rather than a new node added: fog enabled with a cool pale-blue `fog_light_color`, `fog_density 0.012`, `fog_aerial_perspective 0.5`, `fog_sky_affect 0.35`; filmic tonemap; color adjustments enabled with contrast 1.05 / saturation 0.92; ambient energy dropped to 0.8). No gameplay logic — pure atmosphere, and worth an in-editor eyeball next time Thijs is at the machine since fog/tonemap values are aesthetic judgment calls made headless.
- [x] 2026-07-11 — Day/night cycle: `scripts/TimeOfDay.gd` (new autoload, registered in `project.godot` — normalized 0-1 time of day, 0.25 = morning, advancing via `_process` over `DAY_LENGTH_SEC = 600` i.e. one in-game day per 10 real minutes, wrapping at 1.0; `time_changed` signal, `set_time()`/`skip_to_morning()` — the latter is the hook the upcoming bed item needs — plus `sun_elevation()` (1 at noon, negative overnight) and `is_night()` helpers), `scripts/DayNightLight.gd` (new — attached to `Main.tscn`'s existing `DirectionalLight3D`; each frame rotates the sun from `TimeOfDay.normalized_time` — horizon at sunrise, straight down at noon — and scales `light_energy` by clamped `sun_elevation()` so nights go dark), `scenes/world/Main.tscn` (script attached to the light).
- [x] 2026-07-11 — Hunger-zero consequence: `scripts/Player.gd` (new `STARVING_SPEED_MULT = 0.5` const; the frame's movement speed calc was extracted into a probe-able `_current_speed(sprinting)` helper that applies the sprint bonus and then halves the result while `Stats.get_value("hunger") <= 0` — penalty lifts immediately once hunger rises above 0). Gray-box only, no damage/death from starving (Phase 3 concern).
- [x] 2026-07-11 — HUD hunger + stamina bars: `scenes/ui/HUD.tscn` (new `HungerLabel`/`HungerBar` + `StaminaLabel`/`StaminaBar` — `ProgressBar`s, max 100, start full, `show_percentage` off, placed under the chopping label), `scripts/HUD.gd` (subscribes to `Stats.changed`, `_on_stat_changed` matches on stat name and sets the corresponding bar's `value`; `_ready` seeds both bars from `Stats.get_value()` so they're correct before the first signal).
- [x] 2026-07-11 — `Stats.gd` stamina stat: `_apply_delta` renamed to public `add(stat_name, delta)` (used internally by hunger's `_process` decay too, so there's one code path for applying/clamping/emitting stat changes) and a `stamina` entry added to `_values` (starts full, same `[0, MAX_VALUE]` clamp). `scripts/Player.gd` gained `STAMINA_DRAIN_PER_SEC`/`STAMINA_REGEN_PER_SEC` consts (empties in 15s of continuous sprint, refills in 8s while not sprinting) and `_track_sprint()` now calls `Stats.add("stamina", ...)` on both branches — draining while sprinting, regenerating otherwise. No UI or gameplay consequence at 0 stamina yet (e.g. no forced sprint-stop) — that's left for a later queue item once the HUD bar exists to show it.
- [x] 2026-07-11 — `Stats.gd` autoload for hunger: new autoload (registered in `project.godot`, mirrors `Inventory.gd`/`Skills.gd`'s shape) with a `hunger` stat (0-100, starts full), decaying at a fixed rate (`HUNGER_DECAY_PER_SEC` — placeholder pacing of a full deplete over 10 real-time minutes) via `_process`, clamped to `[0, MAX_VALUE]`, `changed(stat_name, new_value)` signal only emitted when the value actually moves, `get_value(stat_name)` read API (unknown stat names default to `MAX_VALUE`, matching `Inventory`'s zero-default convention but for a "full" stat). No UI or gameplay consequence yet — those are separate queue items.
- [x] 2026-07-11 — Placeholder "combat swing" skill-practice verb: `scripts/Player.gd` (`_unhandled_input` now also checks the existing `attack` action, routing to a new bare `_try_attack()` that calls `Skills.practice("combat", 1)` — no hit detection/damage yet, that's Phase 3's job; this just gray-boxes the practice hook so the counter/perks/HUD progress work identically to chopping/running once real combat lands). This was the last remaining Phase 1 item — Phase 1 (skill-by-practice foundation) is now fully complete.
- [x] 2026-07-11 — "Running" skill-practice verb: `project.godot` (new `sprint` input action, bound to Shift), `scripts/Player.gd` (`SPRINT_SPEED` const = `SPEED * 1.6`, `_track_sprint()` accumulates held-time while `sprint` is held *and* the player is actually moving, calling `Skills.practice("running", 1)` every `SPRINT_PRACTICE_INTERVAL` = 3 seconds of continuous hold — releasing sprint or stopping resets the accumulator so only sustained sprinting counts, not tapping the key; movement speed also bumps to `SPRINT_SPEED` while sprinting so the verb is actually felt, not just tracked invisibly).
- [x] 2026-07-11 — HUD chopping skill progress display: `scenes/ui/HUD.tscn` (new `ChoppingLabel`), `scripts/HUD.gd` (subscribes to `Skills.practiced`, refreshes the label as `"Chopping: %d / %d"` using `Skills.get_count()`/`Skills.get_next_threshold()` so the upcoming perk threshold is visible before it's crossed, not just felt via the toast).
- [x] 2026-07-11 — Perk/threshold unlock for chopping + generalized milestone signal: `scripts/Skills.gd` (added `milestone_reached(skill_name, threshold, new_count)` signal, emitted once per threshold crossed inside `practice()`'s existing crossing loop — already skill-name-agnostic, so this also covers the separate "generalize the milestone signal" queue item; no other verb needs its own threshold-check code, they can all just listen to this one signal), `scripts/Player.gd` (chop handler now reads `Skills.get_next_threshold("chopping")` before calling `Skills.practice()`, grants 2 wood instead of 1 if the practice call crossed that threshold), `scripts/HUD.gd` + `scenes/ui/HUD.tscn` (new `ToastLabel` + `ToastTimer` nodes, `HUD.gd` subscribes to `Skills.milestone_reached` and shows a 3-second on-screen toast — "Chopping milestone! (10 practices)" etc. — decoupled from Player, so any future skill's milestones show the same way for free).
- [x] 2026-07-11 — Geometric perk-threshold table in `Skills.gd`: replaced the flat `MILESTONE_INTERVAL = 10` with a widening threshold sequence (`10, 25, 55, 115, 235`, then generated further by `*2.1` and rounding) per the diminishing-returns design locked in Brief section 4. Added `get_next_threshold(skill_name)` for perk/HUD code to read cleanly, and milestone detection now correctly reports every threshold crossed even when a single `practice()` call jumps past more than one (batched amounts).
- [x] 2026-07-11 — Name check for "The Long Apprentice": informal web search against Steam, itch.io, and general trademark/product results — no exact-title collision found (see `The Long Apprentice - Brief.md` section 2 for the write-up and caveats). Formal USPTO TESS search and manual storefront search still recommended before commercial release.
- [x] 2026-07-11 — Placeholder logo / title screen scene: `scenes/ui/TitleScreen.tscn` (new — `Control` root, `ColorRect` background, centered `VBoxContainer` with a large "The Long Apprentice" title `Label` and a "Start" `Button`), `scripts/TitleScreen.gd` (new — connects the Start button's `pressed` signal, calls `get_tree().change_scene_to_file("res://scenes/world/Main.tscn")`), `project.godot` (edited — `run/main_scene` repointed from `Main.tscn` to `TitleScreen.tscn`, so the game now boots to the title screen first).
- [x] 2026-07-11 — Skill-by-practice counter for "chopping": `scripts/Skills.gd` (new autoload singleton, registered in `project.godot`, mirrors `Inventory.gd`'s shape — `Dictionary` of skill name → practice count, `practice(skill_name, amount)` / `get_count(skill_name)`, `practiced` signal, prints a milestone line every 10 practices), `scripts/Player.gd` (edited — chop handler now also calls `Skills.practice("chopping", 1)`).
- [x] 2026-07-11 — Place a block from inventory: `scenes/props/Block.tscn` (new — placeholder `StaticBody3D` cube, group `placed_block`, no script needed yet), `place_block` input action added to `project.godot` (right mouse button), `scripts/Player.gd` `_try_place_block()` raycasts from camera (10m range) and spawns a `Block.tscn` instance at the hit point + surface normal offset, decrementing `Inventory` wood by 1 (no-op if wood count is 0).
- [x] 2026-07-11 — Minimal UI: `scenes/ui/HUD.tscn` (CanvasLayer + Label) + `scripts/HUD.gd`, shows live wood count, instanced in `Main.tscn`.
- [x] 2026-07-10 — Verified Godot 4.3 runs headless in sandbox; `project.godot`, `Player.tscn`, `Main.tscn` import and instantiate without errors.
- [x] 2026-07-10 — Initialized git repo, set up PROGRESS.md and hourly automation.
- [x] 2026-07-10 — "Chop a cube tree" interaction: `scenes/props/Tree.tscn` + `scripts/Tree.gd` (StaticBody3D, group `choppable`, `chop()` emits `chopped` then `queue_free()`s), raycast-based interact in `scripts/Player.gd` (`_unhandled_input` on `interact` action → camera raycast, `INTERACT_RANGE = 3.0` → calls `chop()`). Two Tree instances placed in `scenes/world/Main.tscn` in front of player spawn for manual testing.
- [x] 2026-07-10 — Basic inventory (numbers only): `scripts/Inventory.gd` autoload singleton (registered in `project.godot` `[autoload]`), `Dictionary` of item name → count, `add(item_name, amount)` / `get_count(item_name)`, emits `changed(item_name, new_count)` signal for the future UI to hook into. `Player.gd`'s chop handler now calls `Inventory.add("wood", 1)` instead of tracking a local `wood_count` var.

## Session Log

*(newest first — each hourly run appends one entry)*

- **2026-07-11 (combat verb / Stats.gd hunger+stamina)** — Queue top-up plus 3 implemented items, each validated and committed separately. **Queue top-up:** the Task Queue was down to 1 item (below the 3-item floor per the runbook's step 2), so decomposed **Phase 2 — Survival & atmosphere basics** (Brief section 10) into 7 concrete items and appended them: `Stats.gd` hunger, `Stats.gd` stamina, HUD hunger/stamina bars, hunger-zero consequence, day/night cycle, `WorldEnvironment` mood pass, sleep/bed interaction. **1) Combat swing placeholder verb** (the queue's original last Phase 1 item): `scripts/Player.gd`'s `_unhandled_input` now also checks the existing `attack` action, routing to a new bare `_try_attack()` that calls `Skills.practice("combat", 1)` — no hit detection/damage yet (Phase 3's job). This completes Phase 1 (skill-by-practice foundation) in full. Validated via headless `--import` (clean) plus a throwaway `SceneTree` probe asserting one `_try_attack()` call increments the `"combat"` practice count by 1 — `ALL_OK`, probe deleted after use. **2) `Stats.gd` autoload for hunger:** new autoload (registered in `project.godot`, mirrors `Inventory.gd`/`Skills.gd`'s shape) with a `hunger` stat starting at 100, decaying via `_process` at a fixed rate (placeholder pacing: full deplete over 10 real-time minutes), clamped to `[0, 100]`, `changed` signal only fires when the value actually moves. Validated via headless `--import` (clean) plus a probe asserting the start value, the exact decay amount after 60s, clamping at 0 after a very long tick, and that an unset stat name defaults to `MAX_VALUE` — `ALL_OK`, probe deleted after use. **3) `Stats.gd` stamina stat:** renamed the private `_apply_delta` to a public `add(stat_name, delta)` (hunger's own decay now routes through it too, so there's one shared clamp/emit code path), added a `stamina` entry starting full; `scripts/Player.gd` gained `STAMINA_DRAIN_PER_SEC`/`STAMINA_REGEN_PER_SEC` consts (empties in 15s of continuous sprint, refills in 8s while not sprinting) and `_track_sprint()` now calls `Stats.add("stamina", ...)` on both the sprinting and not-sprinting branches. No UI or 0-stamina gameplay consequence yet — deliberately left for later Phase 2 items once the HUD bar exists to communicate it. Validated via headless `--import` (clean) plus a probe calling `_track_sprint()` directly with synthetic deltas (5s sprint → exact expected drain; 2s not-sprinting → exact expected regen; 1000s sprint → clamps at 0, doesn't go negative) — `ALL_OK`, probe deleted after use. All three commits are local only (not pushed to `origin`) per this file's standing git-note. Queue now has 5 Phase 2 items left (still comfortably above the 3-item floor); next session should pick up "HUD hunger + stamina bars" next since both stats now exist but are invisible to the player.
- **2026-07-11 (chopping perk / HUD progress / running verb)** — Implemented 3 queue items in sequence, each validated and committed separately. **1) Perk/threshold unlock for chopping:** `scripts/Skills.gd` gained a generalized `milestone_reached(skill_name, threshold, new_count)` signal, emitted from the existing threshold-crossing loop inside `practice()` — since it's skill-name-agnostic it also fully satisfies the separate "generalize the milestone signal" queue item, which was removed from the queue as redundant rather than left to be done twice; `scripts/Player.gd`'s chop handler now reads `Skills.get_next_threshold("chopping")` before practicing and grants 2 wood instead of 1 if the practice call crosses it; `scripts/HUD.gd` + `scenes/ui/HUD.tscn` gained a `ToastLabel`/`ToastTimer` pair, subscribed to `Skills.milestone_reached`, showing a 3-second on-screen toast — decoupled from Player, so future skills' milestones get the same toast for free. Validated via headless `--import` (clean) plus a throwaway `SceneTree` probe exercising `Skills.practice()`/`get_next_threshold()`/`milestone_reached` end-to-end (9 practices → no crossing, 10th → crossing detected, signal payload asserted) — `ALL_OK`, probe deleted after use. **2) HUD chopping progress display:** `scenes/ui/HUD.tscn` gained a `ChoppingLabel`, `scripts/HUD.gd` subscribes to `Skills.practiced` and renders `"Chopping: %d / %d"` off `get_count()`/`get_next_threshold()`. Validated via headless `--import` (clean); the signal-driven live-update path itself could **not** be probed the usual way — see the new wrinkle logged in "Notes for future sessions" below (headless one-shot `SceneTree` scripts never pump a frame, so `_ready()`/signal connections never fire automatically on `add_child()`ed nodes, and manually calling `_ready()` causes duplicate-connection errors when the engine's own deferred ready notification eventually fires too) — instead validated structurally (node exists, is a `Label`, correct default text) plus the underlying `Skills` API calls the label formats from, all `ALL_OK`. The actual signal → label-refresh wiring should get a manual click-through in the graphical editor next time Thijs is at his machine. **3) "Running" skill-practice verb:** `project.godot` gained a `sprint` input action (Shift key — checked the `[input]` section first per this file's standing note, confirmed it didn't already exist); `scripts/Player.gd` gained `SPRINT_SPEED` (`SPEED * 1.6`) and `_track_sprint()`, called from `_physics_process`, which accumulates held-time while sprint is held *and* the player is actually moving, calling `Skills.practice("running", 1)` every 3 seconds of continuous hold (releasing sprint resets the accumulator, so tapping doesn't count) and also bumps movement speed while sprinting so the verb is felt, not just tracked invisibly. Validated via headless `--import` (clean) plus a throwaway probe calling `_track_sprint()` directly with synthetic deltas (6×0.5s → 1 practice; reset + 1.0s → still 1; +6.5s more → 2 more practices, batched-interval crossing) — `ALL_OK`, probe deleted after use. All three commits pushed to local git only (not to `origin`) per this file's standing git-note. Queue is now down to 1 item ("combat swing" placeholder); next session should either pick that up directly or top up Phase 1/move into Phase 2 first per the runbook's step 2, since 1 remaining item is below the ~3-item floor.
- **2026-07-11 (perk-threshold table)** — Implemented "Implement geometric perk-threshold table in `Skills.gd`" (picked over the literal top queue item, "Perk/threshold unlock for chopping," per this file's own repeated notes that the table is a hard dependency and should go first). Files touched: `scripts/Skills.gd` (rewrote — removed the flat `MILESTONE_INTERVAL = 10`, added `BASE_THRESHOLDS: Array[int] = [10, 25, 55, 115, 235]` + `THRESHOLD_GROWTH = 2.1`, with `_threshold_for_index()` generating further thresholds beyond the base array by repeated `*2.1` + round; `practice()` now compares thresholds-crossed before/after the increment so a single call — including a batched `amount` — correctly reports every milestone it jumps past, not just whether the new count lands exactly on one; added `get_next_threshold(skill_name)` as the clean read API the still-pending chopping-perk and HUD-progress queue items need). No other files touched — `Player.gd`/`HUD.gd` weren't wired to the new table this session, that's the next two queue items' job. Ran directly on Thijs's Windows machine using the real local editor binary. Validation: headless `--import` clean (no SCRIPT ERROR), plus a throwaway `SceneTree` probe script (`_initialize()` override, `get_root().get_node("Skills")` to reach the autoload since a bare `get_node()` call isn't valid on `SceneTree` itself — new wrinkle vs. prior sessions' `_initialize()`-only note, added below) — asserted `get_count`/`get_next_threshold` at 0, 9, 10, and 30 practices for "chopping" (confirming the 10→25 threshold transition and that going from 9→10 with a single `+1` crosses exactly one threshold), plus a single 300-practice batch call for a fresh "mining" skill (confirming multiple thresholds — 10, 25, 55, 115, 235 — all print in one call and `get_next_threshold` correctly returns something above 300, i.e. the generated-beyond-the-base-array path works). Printed `ALL_OK`; probe script deleted after use. Did not manually playtest in the graphical editor. Queue item "Perk/threshold unlock for chopping" is now unblocked and reworded to reference the new `get_next_threshold()` helper; the HUD queue item was similarly reworded. Next session should pick up "Perk/threshold unlock for chopping" (now the sole remaining Phase 1 blocker-free item at the top) or the HUD progress display — either now reads cleanly off `Skills.gd`.
- **2026-07-11 (name check)** — Implemented the top queue item, a non-code research task: informal name check for "The Long Apprentice" against Steam, itch.io, and general trademark/product search. No exact-title collision found — "Apprentice" alone is a common word in game titles (*The Apprentice*, *Light Apprentice*, *The Toymaker's Apprentice*, *The Dragon Apprentice*, *The Librarian's Apprentice*, *The Fool's Apprentice*, the Trump/NBC TV franchise and its tie-in games), but none combine it with "Long." Findings written into `The Long Apprentice - Brief.md` section 2 (replacing the old TODO bullet), including an explicit caveat that this was informal web search only — not a formal USPTO TESS trademark search or a manual Steam/itch.io storefront search (neither was directly available via this session's tools) — and that a formal check is still recommended before any commercial release (store page, public launch). Also cleaned up section 12's stale "Next Steps" checklist while in the file: checked off three items (`chop tree script`, `title screen`, `skill-decay rules locked`) that were already done per this file's Completed list but hadn't been reflected there, plus the name-check item itself. Files touched: `The Long Apprentice - Brief.md` (sections 2 and 12), `PROGRESS.md` (this entry + queue/Completed changes). No `.gd`/`.tscn` files touched, so no headless-import validation was needed this run. Queue now has 6 items, all code work; next session should pick up "Implement geometric perk-threshold table in `Skills.gd`" first per the prior session's note (the queue's literal top item, "Perk/threshold unlock for chopping," depends on it — do the table before the unlock item even though it's listed second).
- **2026-07-11 (skill-decay design note)** — Implemented the top queue item, a non-code design task: locked down the three open design questions from Brief section 4 (Skill-by-Practice System). Decisions, written into the Brief itself (`The Long Apprentice - Brief.md` section 4, replacing the "Open design questions" bullets with "Design decisions (locked 2026-07-11...)"): (1) **no decay** for v1 — practice counters never decrease on their own; decay would punish players for practicing a *different* verb, which the multi-verb design wants to encourage, and is flagged as a last-resort fix (not the first thing to try) if playtesting later shows skill-hoarding; (2) **diminishing returns via widening perk-threshold spacing**, not via shrinking the per-action counter increment — the raw counter stays a simple flat `+1` per use (keeps `Skills.gd`'s `practice()` trivial and the counter a legible "times done" stat), but perk thresholds grow geometrically (~2.1x each step, e.g. `10, 25, 55, 115, 235...`) instead of the current flat `MILESTONE_INTERVAL = 10`, so grinding one verb yields perks further and further apart over time; (3) **communication is both visible-progress AND an explicit unlock moment**, not felt-only — HUD should show current count (ideally "N / next threshold" once the table exists) and threshold crossings should show an on-screen toast, not just a console print; the underlying mechanical change should also be felt on top of the toast. Files touched: `The Long Apprentice - Brief.md` (section 4 rewritten), `PROGRESS.md` (this entry + queue changes below). No `.gd`/`.tscn` files touched, so no headless-import validation needed this run. Queue changes: removed the completed "Design note" item; added a new item ahead of the existing chopping-perk item — "Implement geometric perk-threshold table in `Skills.gd`" — since the perk-unlock item needs that table to read "next threshold" cleanly, and noted in the perk-unlock item's wording that the unlock message should be an on-screen toast per this design note (it already said this, now cross-referenced explicitly). Next session should pick up "Implement geometric perk-threshold table in `Skills.gd`" (do this before the chopping-perk item, since the perk item depends on it), or the name-check research item if a non-code session is preferred.
- **2026-07-11 (queue top-up)** — No code this session, per step 2 of the runbook: the Task Queue had only 2 items left, both non-code (design note, name check). Reviewed the Completed list against Brief section 10 and confirmed Phase 0 (gray-box prototype: move, chop, pick up wood, place a block) is fully done — the two remaining queue items plus everything already shipped (HUD, title screen, chopping skill counter) actually put the project already into Phase 1 territory. Decomposed **Phase 1 — Skill-by-practice foundation** into 5 new concrete queue items (appended after the existing 2): a chopping perk/threshold unlock communicated to the player, a HUD display of the chopping practice count, a "running" skill-practice verb, a placeholder "combat swing" skill-practice verb (using the already-existing `attack` input action, no hit detection needed yet), and a generalization of `Skills.gd`'s milestone signal so perk code doesn't hardcode thresholds per-verb. Updated Current Status to reflect Phase 1 is now the active phase. Did not touch any `.gd`/`.tscn` files, so no headless-import validation was needed this run. Next session should pick up the top queue item ("Design note: skill-decay and diminishing-returns rules") or, if a code slice is preferred, jump to "Perk/threshold unlock for chopping" — both are now unblocked.
- **2026-07-11 (title screen)** — Implemented "Placeholder logo / title screen scene" (top queue item). Files touched: `scenes/ui/TitleScreen.tscn` (new — `Control` root filling the viewport, `ColorRect` dark background, `CenterContainer` > `VBoxContainer` with a 48pt "The Long Apprentice" `Label` and a "Start" `Button`), `scripts/TitleScreen.gd` (new — `@onready` reference to the Start button, connects `pressed` in `_ready()`, `_on_start_pressed()` calls `get_tree().change_scene_to_file("res://scenes/world/Main.tscn")`), `project.godot` (edited — `run/main_scene` changed from `res://scenes/world/Main.tscn` to `res://scenes/ui/TitleScreen.tscn`). Ran directly on Thijs's Windows machine using the real local editor binary (`C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`). Validation: headless `--import` clean (no SCRIPT ERROR), plus a throwaway `SceneTree` probe script (`_initialize()` override, deleted after use) loaded/instantiated `TitleScreen.tscn` (asserted the `StartButton` node path resolves), `Main.tscn`, `Player.tscn`, `Tree.tscn`, `Block.tscn`, `HUD.tscn` — printed `ALL_OK`. Note: the probe only checks structural instantiation, not the actual `_ready()`-wired button-press → scene-change flow (nodes outside the tree don't get `_ready()` called), since headless `SceneTree` scripts don't run a full frame loop here — worth a manual click-through in the graphical editor next time Thijs is at his machine to confirm Start actually transitions to `Main.tscn`. Queue item "Name check: Steam/itch.io + trademark search for 'The Long Apprentice'" moved up; queue now has 2 items left, so next session should top it up per step 2 (decompose the next unstarted Phase) before or after picking an item. Next session should pick up "Design note: skill-decay and diminishing-returns rules" or do Phase decomposition first since the queue is short.
- **2026-07-11 (skill counter)** — Implemented "Skill-by-practice counter for chopping" (top queue item). Files touched: `scripts/Skills.gd` (new — autoload singleton, `Dictionary` of skill name → count, `practice(skill_name, amount)` increments and emits `practiced(skill_name, new_count)`, `get_count(skill_name)`, prints `"Skill milestone: <name> practiced <n> times"` every `MILESTONE_INTERVAL = 10` — no levels/perks yet, deliberately just a visible counter per the queue item's scope; perk hookup and decay/diminishing-returns rules are separate queue items still pending the design-note task), `project.godot` (edited — added `Skills="*res://scripts/Skills.gd"` to `[autoload]`, alongside `Inventory`), `scripts/Player.gd` (edited — `_try_interact()`'s chop branch now also calls `Skills.practice("chopping", 1)` right after `Inventory.add("wood", 1)`). Ran directly on Thijs's Windows machine using the real local editor binary (`C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`). Validation: headless `--import` clean (no SCRIPT ERROR), plus a throwaway `SceneTree` probe script (`_initialize()` override, deleted after use) loaded/instantiated `Player.tscn`, `Main.tscn`, `Tree.tscn`, `Block.tscn`, `HUD.tscn`, then exercised `Skills` end-to-end via `get_node("/root/Skills")` (10 practices → milestone line printed, `get_count("chopping") == 10` asserted) — printed `ALL_OK`. **New wrinkle for future probe scripts:** unlike scenes that reference an autoload from deep inside their own script body (fine to `load()` from `_initialize()`), a probe script that is itself the `--script` entry point and references an autoload identifier *directly in its own top-level code* fails with `SCRIPT ERROR: Identifier not found` at compile time, because the entry script is parsed before autoloads are registered — even under `_initialize()`. Workaround: reach the singleton via `get_node("/root/<Name>")` (or `Engine.get_singleton`) instead of the bare global identifier when writing the entry probe script itself. Did not manually playtest in the graphical editor — worth confirming the milestone print actually fires after 10 real chops next time Thijs is in the editor. Next session should pick up "Placeholder logo / title screen scene".
- **2026-07-11 (place block)** — Implemented "Place a block from inventory" (top queue item). Files touched: `scenes/props/Block.tscn` (new — `StaticBody3D` with `BoxMesh`/`BoxShape3D`, 1x1x1, group `placed_block`, no script — follows the Brief's "never place raw meshes, build a reusable scene" rule but doesn't need chop-style behavior yet), `project.godot` (edited — added `place_block` input action bound to right mouse button, alongside existing `attack` on left mouse), `scripts/Player.gd` (edited — added `PLACE_RANGE = 10.0` const, `BLOCK_SCENE` preload, routed `place_block` action through `_unhandled_input` to new `_try_place_block()`: raycasts from camera, bails early if `Inventory.get_count("wood") <= 0`, otherwise instances `Block.tscn` under `get_tree().current_scene`, positions it at `result.position + result.normal * 0.5` so it sits flush on the hit surface, and decrements wood via `Inventory.add("wood", -1)`). Ran directly on Thijs's Windows machine using the real local editor binary (`C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`). Validation: headless `--import` clean (no SCRIPT ERROR), plus a throwaway `SceneTree` probe script (`_initialize()` override, deleted after use) loaded/instantiated `Block.tscn`, `Player.tscn`, `Main.tscn`, `Tree.tscn` — printed `ALL_OK`. `config/features` still reads `4.7` after import (expected, not drift). Did not manually playtest in the graphical editor (no interactive session this run) — worth a visual check next time Thijs is in the editor, particularly whether the block placement offset/orientation feels right when placing against walls vs. the ground. Placement currently has no max-distance-from-player check beyond the 10m raycast and no collision-overlap check against existing blocks/trees (could let blocks embed into other geometry) — fine for this placeholder pass, flag if it becomes a problem. Next session should pick up "Skill-by-practice counter for chopping".
- **2026-07-11 (wood HUD)** — Implemented "Minimal UI: wood count label" (top queue item). Files touched: `scripts/HUD.gd` (new — `CanvasLayer` script, connects to `Inventory.changed`, updates a `Label` with `"Wood: %d"`), `scenes/ui/HUD.tscn` (new — `CanvasLayer` root with a `WoodLabel` child in the top-left corner), `scenes/world/Main.tscn` (edited — added `ext_resource` for `HUD.tscn` and instanced it as a child of `Main`). Ran directly on Thijs's Windows machine using the real local editor binary (`C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`), not the sandboxed Linux cache. Validation: headless `--import` clean (no SCRIPT ERROR), plus a throwaway `SceneTree` probe script (deleted after use) loaded/instantiated `HUD.tscn`, `Main.tscn`, `Player.tscn`, `Tree.tscn`. **Note for future sessions:** the probe script must override `_initialize()`, not `_init()` — calling `ResourceLoader.load()` on scripts that reference the `Inventory` autoload from `_init()` throws `Identifier not found: Inventory` because autoload singleton globals aren't registered yet at that point in a `--script`-driven headless run; `_initialize()` runs after autoload setup and resolves fine. Committed this session's changes (`scenes/ui/HUD.tscn`, `scripts/HUD.gd`, `scripts/HUD.gd.uid`, `scenes/world/Main.tscn`, `PROGRESS.md`). Did not manually playtest in the graphical editor (headless-only environment) — worth a quick visual check next time Thijs is in the editor. Next session should pick up "Place a block from inventory".
- **2026-07-10 (inventory)** — Implemented "Basic inventory (numbers only)" (top queue item). Files touched: `scripts/Inventory.gd` (new — autoload singleton, `add()`/`get_count()`, `changed` signal), `project.godot` (edited — added `[autoload]` section registering `Inventory`), `scripts/Player.gd` (edited — chop handler now calls `Inventory.add("wood", 1)` / `Inventory.get_count("wood")` instead of a local `wood_count` var). Also deleted the two stray placeholder files noted as safe to remove (`scripts/_probe_test.gd`, `_validate.gd` + their `.uid` files) since this run had normal filesystem access. Found the previous session's uncommitted "chop tree" work already committed as `2c9ae96 first claude change` at the start of this run (git and the real Windows Godot editor both work fine in this environment — the sandbox mount-cache/git issues from the prior session's notes didn't reproduce here). Validation: headless `--import` clean (no SCRIPT ERROR output), plus a throwaway `SceneTree` probe script (deleted after use) loaded/instantiated `Tree.tscn`, `Main.tscn`, `Player.tscn` and exercised the `Inventory` autoload end-to-end (`add`/`get_count` round-tripped correctly) — printed `ALL_OK`. Committed this session's changes. Next session should pick up "Minimal UI: wood count label".
- **2026-07-10 (chop tree)** — Implemented "Chop a cube tree" (top queue item). Files touched: `scripts/Tree.gd` (new), `scenes/props/Tree.tscn` (new), `scripts/Player.gd` (edited — added `_try_interact()` raycast + `wood_count`), `scenes/world/Main.tscn` (edited — added 2 Tree instances). Validation passed: headless `--import` clean, and a throwaway `SceneTree` script (`ResourceLoader.load` + `.instantiate()` on Tree.tscn/Main.tscn/Player.tscn) reported `ALL_OK` for all three. **Blocker hit and worked around:** the sandbox's bash mount of the project folder served stale/truncated cached content for files that already existed before this session (`Player.gd`, `Main.tscn`) even well after edits via the file-editing tool and after long waits — reads consistently returned an identical mid-line truncation, while brand-new files synced immediately. Worked around by writing the final content for those two files directly via bash heredoc (content matches what's shown above / in the files now) — bash self-consistent read-after-write worked fine, only the cross-tool sync was stale. Two harmless stray files were created while diagnosing this and could not be deleted (`rm` is blocked on this mount, same as the known `.git` issue): `scripts/_probe_test.gd` and `_validate.gd` (project root) — both overwritten with inert comment-only placeholders so they don't affect Godot's script parsing. **These two files are safe to delete manually** next time someone has normal filesystem access; they aren't referenced anywhere. **Git unavailable this run** — `git status` still fails with "unknown error occurred while reading the configuration files" and `.git/config` is unreadable via `cat` despite `ls`/`stat` showing it exists (same known stale-mount-cache issue noted in the Validation section, not a real repo problem). Did not touch `.git`. Nothing committed — next session with a working git should commit: `scripts/Tree.gd`, `scenes/props/Tree.tscn`, `scripts/Player.gd`, `scenes/world/Main.tscn`, `PROGRESS.md`, and ideally delete the two stray placeholder files. Next session should pick up "Basic inventory (numbers only)" — a good time to migrate `wood_count` off Player and into the new autoload.
- **2026-07-10 (setup)** — Created this file, initialized git, set up hourly scheduled task. No feature work yet — next session should start on "Chop a cube tree."

## Notes for future sessions

- A `SceneTree`-entry probe script can't call bare `get_node()` on itself (it's not in the scene tree as a node) — use `get_root().get_node("<AutoloadName>")` to reach an autoload singleton from inside `_initialize()`.
- SceneTree probe scripts used for validation must override `_initialize()`, not `_init()` — autoload singletons (e.g. `Inventory`) aren't registered as script globals yet during `_init()` in a `--script`-driven headless run, so referencing them there throws `Identifier not found`. `_initialize()` runs after autoload setup.
- That said, a probe script that IS the `--script` entry point still fails if it references an autoload by its bare global identifier anywhere in its own top-level code (even inside `_initialize()`) — the entry script itself is parsed/compiled before autoloads are registered, so this is a compile-time error, not a runtime one. Fix: use `get_node("/root/<AutoloadName>")` in the entry probe script instead of the bare identifier. (Scenes/scripts loaded via `ResourceLoader.load()` *from* the probe don't have this problem — they compile lazily at load time, after autoloads exist.)
- Engine: Godot 4.7 stable, GDScript. Sessions run directly on Thijs's persistent Windows machine and validate using his actual local editor binary (`C:\Users\Thijs\Downloads\Godot_v4.7-stable_win64.exe\Godot_v4.7-stable_win64_console.exe`) — not a separate sandbox/VM. A cached Linux binary exists at `.godot-tools/` as an untested fallback in case a future session ever runs in a genuinely ephemeral environment.
- Follow the folder structure and conventions in `The Long Apprentice - Brief.md` (scenes/player, scenes/world, scenes/props, scripts, assets, addons).
- Reusable-component rule from the Brief: never place raw meshes directly — build a scene once (e.g. `Tree.tscn`), instance it everywhere.
- Input actions aren't fully defined yet in the Input Map beyond what `Player.gd` assumes (`move_forward/back/left/right`, `jump`) — check `project.godot` `[input]` section before assuming an action exists; add missing ones there.
- Stronger version of the wrinkle below (confirmed 2026-07-11 during the chase-AI probe): nodes `add_child()`ed during `_initialize()` are not inside the tree AT ALL yet — `get_tree()` on them returns null (so `get_first_node_in_group()` etc. crash), and `global_position` reads/writes error with `!is_inside_tree()` and return identity transforms. Tree entry only happens once the main loop starts, which a one-shot probe never does. Consequence: logic that needs tree lookups or global transforms can't be exercised through live nodes headless — factor it into pure/static functions of plain values (e.g. `Enemy._chase_direction(from_pos, target_pos)`) and probe those instead.
- Also confirmed 2026-07-11: a failed `assert()` in a probe script halts the script BEFORE `quit()` runs, so the headless process never exits — it hangs, it does not report-and-exit. Write probes that collect failures into an array and always `quit()` at the end (print `ALL_OK` or `FAILURES: [...]`), rather than raw asserts.
- In a headless one-shot probe, a node `add_child()`ed during `_initialize()` is not yet "inside the world" — calling `get_world_3d()` on it (e.g. via `Player._camera_raycast()`) prints an `!is_inside_world()` error and the raycast returns `{}`. Benign probe artifact, not a runtime bug (real input can only fire after the node has entered the world); treat the empty result as the miss path.
- Headless one-shot `SceneTree` probe scripts never pump a frame, so `NOTIFICATION_READY`/`_ready()` never fires automatically on nodes added via `add_child()` (confirmed 2026-07-11 while probing `HUD.gd`'s new signal-driven label — `is_connected()` came back `false` after `add_child`, and manually calling `hud._ready()` produced duplicate-connection errors later when the engine's own deferred ready notification eventually fired too). Don't try to work around this by calling `_ready()` manually or awaiting a frame; instead, for probing signal-wired UI/behavior code, either (a) exercise the underlying autoload API directly (e.g. `Skills.get_count()`/`get_next_threshold()`) and treat the signal-handler method as a thin formatter you trust by reading it, or (b) just verify structural instantiation (node paths/types/default values) like prior sessions did, and note that the live wiring needs a manual click-through in the graphical editor.
- Keep each of the 3 items scoped — one small vertical slice per item, validated and committed individually before moving to the next. Don't let item 3 balloon into a second milestone.
- If genuinely blocked (missing decision, unclear design), don't guess silently — leave a clear note in the Session Log describing the blocker so the next session (or Thijs) can resolve it.
